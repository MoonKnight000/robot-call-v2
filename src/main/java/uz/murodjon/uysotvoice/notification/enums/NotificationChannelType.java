package uz.murodjon.uysotvoice.notification.enums;

/** {@code notification_channel.channel}/{@code notification_matrix.channel} (§11 settings). */
public enum NotificationChannelType {
    EMAIL,
    WEBHOOK,
    TELEGRAM
}
