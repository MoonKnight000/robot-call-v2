package uz.murodjon.robotcallv2.aiagent.domain.enums;

/**
 * Presets a new agent can be started from, so the first screen is not empty.
 */
public enum AgentTemplate {
    BUSINESS,
    SUPPORT,
    MEDICAL,
    BLANK
}
