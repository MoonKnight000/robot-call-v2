package uz.murodjon.robotcallv2.donotcall.domain.enums;

/** Source of a DNC entry (§10.8): call agent action, CRM sync, or manual panel entry. */
public enum DoNotCallSource {
    CALL,
    CRM,
    MANUAL
}
