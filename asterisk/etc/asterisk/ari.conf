; ARI users. The voice-agent-service connects with these credentials.
; NOTE: replace the password before any non-local use. Keep in sync with the
; application's ARI_USER / ARI_PASSWORD env vars (PROJECT.md §12).
[general]
enabled = yes
pretty = yes
; allowed_origins = *   ; uncomment if connecting from a browser-based client

[uysot]
type = user
read_only = no
password = CHANGE_ME_ARI_PASSWORD
